# 🔧 Отладка SendGrid в Vercel

## ❌ Проблема: Email не отправляются из Vercel

Если вы добавили переменные в Vercel, но email не отправляются, проверьте следующее:

## ✅ Шаг 1: Проверьте переменные окружения в Vercel

### Как проверить:

1. Зайдите в **Vercel Dashboard**
2. Выберите ваш проект
3. Перейдите в **Settings** → **Environment Variables**
4. Убедитесь, что есть **ОБЕ** переменные:

   ```
   SENDGRID_API_KEY = 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
   FROM_EMAIL = info@studiozorina.com
   ```

### ⚠️ Важно:

- **Имена переменных должны быть ТОЧНО такими** (чувствительны к регистру):
  - `SENDGRID_API_KEY` (не `sendgrid_api_key` или `SENDGRID-API-KEY`)
  - `FROM_EMAIL` (не `from_email` или `FROM-EMAIL`)

- **Выберите правильные окружения:**
  - ✅ **Production** (для продакшена)
  - ✅ **Preview** (для preview деплоев)
  - ✅ **Development** (опционально)

- **После добавления/изменения переменных:**
  - Vercel должен автоматически перезапустить деплой
  - Если нет - нажмите **Redeploy** вручную

## ✅ Шаг 2: Проверьте логи Vercel

### Как проверить логи:

1. В Vercel Dashboard → ваш проект
2. Перейдите в **Deployments**
3. Выберите последний деплой
4. Откройте **Functions** → **Logs**
5. Ищите сообщения об email:

   **Если переменные НЕ найдены:**
   ```
   ℹ️ SendGrid API key not configured. Would send referral code to...
   To enable email sending, configure SENDGRID_API_KEY environment variable
   ```

   **Если переменные найдены, но есть ошибка:**
   ```
   ❌ Error sending referral email to...
   SendGrid error details: ...
   ```

## ✅ Шаг 3: Проверьте верификацию домена в SendGrid

### Как проверить:

1. Зайдите в [SendGrid Dashboard](https://app.sendgrid.com/)
2. Перейдите в **Settings** → **Sender Authentication**
3. Проверьте статус домена `studiozorina.com`:
   - ✅ **Verified** - домен подтвержден
   - ⚠️ **Pending** - ожидает подтверждения
   - ❌ **Unverified** - не подтвержден

### Если домен не верифицирован:

1. Нажмите **Authenticate Your Domain**
2. Добавьте DNS записи, которые просит SendGrid
3. Дождитесь подтверждения (может занять до 48 часов)

## ✅ Шаг 4: Проверьте SendGrid Activity

### Как проверить:

1. В SendGrid Dashboard → **Activity**
2. Проверьте последние отправки:
   - ✅ **Delivered** - email доставлен
   - ⚠️ **Bounced** - email отскочил
   - ❌ **Blocked** - email заблокирован
   - ⚠️ **Deferred** - отложен

### Если email заблокирован:

- Проверьте причину в SendGrid
- Убедитесь, что домен верифицирован
- Проверьте, что email адрес получателя валидный

## ✅ Шаг 5: Проверьте API ключ SendGrid

### Как проверить:

1. В SendGrid Dashboard → **Settings** → **API Keys**
2. Найдите ваш API ключ (начинается с `SG.w2qWtwKETZ...`)
3. Проверьте:
   - ✅ **Status**: Active
   - ✅ **Permissions**: Mail Send (должен быть включен)

### Если ключ неактивен:

1. Создайте новый API ключ
2. Выберите **Full Access** или **Restricted Access** с правом **Mail Send**
3. Обновите `SENDGRID_API_KEY` в Vercel

## ✅ Шаг 6: Тест через API endpoint

Создайте тестовый endpoint для проверки:

```javascript
// app/api/test-email/route.js
import { sendReferralCodeEmail } from '@/lib/email-service-simple'

export async function GET(request) {
  const testEmail = request.nextUrl.searchParams.get('email') || 'test@example.com'
  
  const result = await sendReferralCodeEmail(
    'Test User',
    testEmail,
    'TEST123',
    'https://zorinastudio-referral.com/ref/TEST123'
  )
  
  return Response.json({
    success: result.success,
    messageId: result.messageId,
    error: result.error,
    env: {
      hasApiKey: !!process.env.SENDGRID_API_KEY,
      hasFromEmail: !!process.env.FROM_EMAIL,
      fromEmail: process.env.FROM_EMAIL
    }
  })
}
```

Затем вызовите:
```
https://your-domain.vercel.app/api/test-email?email=your-email@example.com
```

## 🔍 Частые проблемы и решения:

### Проблема 1: "SendGrid API key not configured"

**Причина:** Переменная `SENDGRID_API_KEY` не найдена в Vercel

**Решение:**
1. Проверьте имя переменной (должно быть точно `SENDGRID_API_KEY`)
2. Убедитесь, что выбрано правильное окружение (Production)
3. Перезапустите деплой после добавления переменной

### Проблема 2: "Forbidden" или "401 Unauthorized"

**Причина:** Неверный API ключ или ключ не имеет прав

**Решение:**
1. Проверьте API ключ в SendGrid Dashboard
2. Убедитесь, что ключ активен
3. Проверьте права ключа (должно быть Mail Send)

### Проблема 3: "The from address does not match a verified Sender Identity"

**Причина:** Домен или email не верифицирован в SendGrid

**Решение:**
1. Верифицируйте домен `studiozorina.com` в SendGrid
2. Или используйте верифицированный email адрес

### Проблема 4: Email отправляется, но не доставляется

**Причина:** Проблемы с доставляемостью

**Решение:**
1. Проверьте SendGrid Activity для деталей
2. Проверьте спам папку получателя
3. Убедитесь, что домен имеет хорошую репутацию

## 📋 Чеклист для проверки:

- [ ] Переменные добавлены в Vercel с правильными именами
- [ ] Выбраны правильные окружения (Production, Preview)
- [ ] Деплой перезапущен после добавления переменных
- [ ] Домен верифицирован в SendGrid
- [ ] API ключ активен и имеет права Mail Send
- [ ] FROM_EMAIL соответствует верифицированному домену
- [ ] Проверены логи Vercel на наличие ошибок
- [ ] Проверена SendGrid Activity для статуса отправки

## 🆘 Если ничего не помогает:

1. **Проверьте логи Vercel** - там должна быть точная ошибка
2. **Проверьте SendGrid Activity** - там видно статус отправки
3. **Создайте тестовый endpoint** (см. Шаг 6) для отладки
4. **Свяжитесь с поддержкой SendGrid** если проблема на их стороне

---

**Важно:** После добавления переменных в Vercel, обязательно перезапустите деплой или дождитесь автоматического перезапуска!

