# Vercel Environment Variables - Checklist

## ✅ Проверьте каждый пункт:

### 1. Переменные добавлены в Vercel
- [ ] Откройте: https://vercel.com/dashboard
- [ ] Ваш проект → Settings → Environment Variables
- [ ] Видны ли все 6 переменных?

### 2. Имена переменных ТОЧНО правильные (без пробелов!)
- [ ] `APPLE_PASS_TYPE_ID` (не `APPLE_PASS_TYPE_ID ` с пробелом)
- [ ] `APPLE_PASS_CERTIFICATE_PASSWORD`
- [ ] `APPLE_PASS_TEAM_ID`
- [ ] `APP_BASE_URL`
- [ ] `APPLE_PASS_CERTIFICATE_BASE64`
- [ ] `APPLE_WWDR_CERTIFICATE_BASE64`

### 3. Значения правильные
- [ ] `APP_BASE_URL` = `https://zorinastudio-referral.com` (БЕЗ `/` в конце!)
- [ ] `APPLE_PASS_CERTIFICATE_BASE64` = очень длинный base64 текст (не обрезан)
- [ ] `APPLE_WWDR_CERTIFICATE_BASE64` = base64 текст

### 4. Environment = Production (КРИТИЧНО!)
- [ ] Для КАЖДОЙ переменной в колонке "Environment" есть галочка на **Production**
- [ ] Если галочки нет - переменная НЕ применяется!

### 5. Деплой после добавления
- [ ] После добавления переменных Vercel начал новый деплой?
- [ ] Deployments → Последний деплой → Статус = ✅ Ready?
- [ ] Если нет - сделайте Redeploy вручную

### 6. Проверка логов
- [ ] Deployments → Последний деплой → View Function Logs
- [ ] Есть ли ошибки связанные с `APPLE_PASS`?

## 🔧 Если переменные добавлены, но не работают:

### Вариант 1: Передеплой
1. Deployments → Последний деплой
2. "..." → "Redeploy"
3. Убедитесь "Use existing Build Cache" = OFF
4. Нажмите "Redeploy"

### Вариант 2: Удалить и добавить заново
1. Удалите все 6 переменных
2. Добавьте их заново
3. Убедитесь Production выбрано
4. Сохраните
5. Дождитесь деплоя

### Вариант 3: Проверка через Vercel CLI
Если установлен:
```bash
vercel env ls
```

## 📝 Быстрая проверка после деплоя

Подождите 2-3 минуты после деплоя, затем:

```bash
curl -I "https://zorinastudio-referral.com/api/wallet/pass/2A47E49DFEAC4394"
```

Должен вернуть:
- `200 OK` или `500` (но не `404`)
- `Content-Type: application/vnd.apple.pkpass` (при успехе)

Если `404` - route не найден (проблема с деплоем)
Если `500` - переменные не применены (проверьте Production галочки)

