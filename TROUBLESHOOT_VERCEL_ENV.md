# Troubleshooting Vercel Environment Variables

## ❌ Проблема

Endpoint возвращает 500 ошибку:
```
"Apple Wallet certificates not configured"
```

Это означает, что переменные **НЕ применены** на production сервере.

## ✅ Пошаговая проверка

### Шаг 1: Откройте Vercel Dashboard

1. Перейдите: https://vercel.com/dashboard
2. Выберите ваш проект
3. Settings → **Environment Variables**

### Шаг 2: Проверьте каждую переменную

Убедитесь, что видны **ВСЕ 6 переменных**:

1. ✅ `APPLE_PASS_TYPE_ID`
2. ✅ `APPLE_PASS_CERTIFICATE_PASSWORD`
3. ✅ `APPLE_PASS_TEAM_ID`
4. ✅ `APP_BASE_URL`
5. ✅ `APPLE_PASS_CERTIFICATE_BASE64`
6. ✅ `APPLE_WWDR_CERTIFICATE_BASE64`

### Шаг 3: КРИТИЧНО - Проверьте Environment

Для **КАЖДОЙ** переменной в колонке "Environment" должна быть галочка на:
- ✅ **Production** (обязательно!)

Если галочки нет - переменная НЕ применяется к production!

### Шаг 4: Если переменных нет или нет галочки Production

1. **Добавьте переменную заново:**
   - Нажмите "Add New"
   - Вставьте имя (например: `APPLE_PASS_TYPE_ID`)
   - Вставьте значение
   - **ВАЖНО:** Поставьте галочку на **Production**
   - Нажмите "Save"

2. **Повторите для всех 6 переменных**

### Шаг 5: После добавления/изменения

1. Vercel должен автоматически начать новый деплой
2. Перейдите в **Deployments**
3. Дождитесь завершения (зеленая галочка)
4. Обычно это занимает 2-5 минут

### Шаг 6: Если деплой не начался автоматически

1. Deployments → Последний деплой
2. Нажмите "..." (три точки)
3. Выберите **"Redeploy"**
4. Убедитесь, что выбрано **"Use existing Build Cache"** = OFF
5. Нажмите "Redeploy"

### Шаг 7: Проверьте логи деплоя

1. Deployments → Последний деплой
2. Откройте **"Build Logs"** или **"Function Logs"**
3. Ищите ошибки связанные с:
   - `APPLE_PASS`
   - `certificate`
   - `environment variable`

## 🔍 Альтернативная проверка

Если у вас установлен Vercel CLI:

```bash
vercel env ls
```

Должны быть видны все переменные с пометкой `Production`.

## ⚠️ Частые ошибки

1. **Переменные добавлены, но не для Production**
   - Решение: Добавьте галочку Production для каждой переменной

2. **Переменные добавлены, но деплой не перезапустился**
   - Решение: Сделайте Redeploy вручную

3. **Неправильное значение APP_BASE_URL**
   - Должно быть: `https://zorinastudio-referral.com`
   - БЕЗ `/` в конце!

4. **Base64 сертификаты обрезаны**
   - Убедитесь, что скопировали ВЕСЬ текст base64
   - Он должен быть очень длинным

## ✅ После исправления

Подождите 2-3 минуты после деплоя, затем проверьте:

```bash
node scripts/test-wallet-endpoint.js 2A47E49DFEAC4394 https://zorinastudio-referral.com
```

Должно быть: `✅ Success! Pass file generated`

