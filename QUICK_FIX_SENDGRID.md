# 🚀 Быстрое решение проблемы с SendGrid в Vercel

## ✅ Что проверить в первую очередь:

### 1. Правильность имен переменных в Vercel

В Vercel Dashboard → Settings → Environment Variables должны быть **ТОЧНО** такие имена:

```
SENDGRID_API_KEY
FROM_EMAIL
```

**НЕ:**
- ❌ `sendgrid_api_key` (маленькие буквы)
- ❌ `SENDGRID-API-KEY` (дефисы)
- ❌ `SendGrid_Api_Key` (смешанный регистр)

### 2. Значения переменных

```
SENDGRID_API_KEY = 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
FROM_EMAIL = info@studiozorina.com
```

### 3. Окружения (Environments)

Выберите **ВСЕ** окружения:
- ✅ Production
- ✅ Preview  
- ✅ Development

### 4. Перезапуск деплоя

После добавления переменных:
1. Vercel должен автоматически перезапустить деплой
2. Если нет - нажмите **Redeploy** вручную
3. Дождитесь завершения деплоя

## 🧪 Тест через API endpoint

После деплоя протестируйте:

```
https://your-domain.vercel.app/api/test-email?email=ваш-email@example.com
```

Этот endpoint покажет:
- ✅ Найдены ли переменные окружения
- ✅ Успешно ли отправлен email
- ✅ Детали ошибки (если есть)

## 📋 Чеклист:

- [ ] Переменные добавлены с правильными именами (SENDGRID_API_KEY, FROM_EMAIL)
- [ ] Значения скопированы правильно (без лишних пробелов)
- [ ] Выбраны все окружения (Production, Preview, Development)
- [ ] Деплой перезапущен после добавления переменных
- [ ] Домен верифицирован в SendGrid Dashboard
- [ ] API ключ активен в SendGrid

## 🔍 Где проверить логи:

1. **Vercel Logs:**
   - Dashboard → Deployments → последний деплой → Functions → Logs

2. **SendGrid Activity:**
   - SendGrid Dashboard → Activity → Email Activity

## ⚠️ Частая ошибка:

Если видите в логах:
```
ℹ️ SendGrid API key not configured
```

Это значит, что переменная `SENDGRID_API_KEY` **НЕ найдена** в Vercel.

**Решение:**
1. Проверьте имя переменной (должно быть точно `SENDGRID_API_KEY`)
2. Проверьте, что выбрано окружение Production
3. Перезапустите деплой

---

**Если проблема не решена**, смотрите подробную инструкцию в `SENDGRID_VERCEL_DEBUG.md`

