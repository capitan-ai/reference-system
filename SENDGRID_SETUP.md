# 📧 SendGrid Email Setup Guide

## ✅ Что уже сделано:

1. ✅ Установлен пакет `@sendgrid/mail`
2. ✅ Обновлен код `lib/email-service-simple.js` для использования SendGrid
3. ✅ Создан тестовый скрипт `scripts/test-sendgrid.js`

## 🔧 Шаг 1: Добавить переменные окружения в Vercel

Откройте Vercel Dashboard → Ваш проект → Settings → Environment Variables

### Добавьте следующие переменные:

#### 1. SENDGRID_API_KEY
- **Name:** `SENDGRID_API_KEY`
- **Value:** `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`
- **Environment:** Production, Preview, Development
- **Description:** API ключ SendGrid для отправки email

#### 2. FROM_EMAIL
- **Name:** `FROM_EMAIL`
- **Value:** `info@studiozorina.com`
- **Environment:** Production, Preview, Development
- **Description:** Email адрес отправителя (должен быть верифицирован в SendGrid)

### Опциональные переменные:

#### 3. DISABLE_EMAIL_SENDING (опционально)
- **Name:** `DISABLE_EMAIL_SENDING`
- **Value:** `false` (или `true` для отключения отправки)
- **Environment:** Development (для тестов)
- **Description:** Отключает отправку email (полезно для тестирования)

## 📝 Шаг 2: Добавить переменные в локальный .env файл

Создайте или обновите файл `.env` в корне проекта:

```bash
# SendGrid Configuration
SENDGRID_API_KEY=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
FROM_EMAIL=info@studiozorina.com

# Опционально
DISABLE_EMAIL_SENDING=false
EMAIL_ENABLED=true
```

## ✅ Шаг 3: Проверить верификацию домена в SendGrid

Убедитесь, что домен `studiozorina.com` верифицирован в SendGrid:

1. Зайдите в [SendGrid Dashboard](https://app.sendgrid.com/)
2. Перейдите в **Settings** → **Sender Authentication**
3. Проверьте, что домен `studiozorina.com` подтвержден
4. Убедитесь, что email `info@studiozorina.com` находится в списке verified senders

Если домен не верифицирован:
- Следуйте инструкциям SendGrid для верификации домена
- Добавьте необходимые DNS записи
- Дождитесь подтверждения (может занять до 48 часов)

## 🧪 Шаг 4: Протестировать отправку email

### Локальный тест:

```bash
# Замените your-email@example.com на ваш email
node scripts/test-sendgrid.js your-email@example.com
```

Или установите `TEST_EMAIL` в `.env`:
```bash
TEST_EMAIL=your-email@example.com
```

Затем запустите:
```bash
node scripts/test-sendgrid.js
```

### Ожидаемый результат:

```
🧪 Testing SendGrid email integration...

📤 Sending test email to: your-email@example.com
📧 From: info@studiozorina.com

✅ Referral email sent to your-email@example.com

📊 Result:
{
  "success": true,
  "messageId": "sent"
}

✅ Email sent successfully!
   Check your inbox (and spam folder)
```

## 📊 Что изменилось в коде:

### До (Gmail):
- Использовал `nodemailer` с Gmail SMTP
- Требовал `BUSINESS_EMAIL` и `GMAIL_APP_PASSWORD`
- Функция `createTransporter()` создавала SMTP транспортер

### После (SendGrid):
- Использует `@sendgrid/mail` SDK
- Требует только `SENDGRID_API_KEY` и `FROM_EMAIL`
- Функция `initSendGrid()` инициализирует SendGrid API
- Более надежная доставляемость email
- Лучшая аналитика и отслеживание

## 🔄 Функции, которые теперь используют SendGrid:

1. **`sendReferralCodeEmail()`** - Отправка реферальных кодов клиентам
2. **`sendGiftCardIssuedEmail()`** - Отправка уведомлений о подарочных картах

## ⚠️ Важные замечания:

1. **Верификация домена обязательна** - Без верификации домена SendGrid может блокировать отправку
2. **API ключ должен иметь права Mail Send** - Убедитесь, что API ключ имеет необходимые права
3. **Rate Limits** - SendGrid имеет лимиты на количество отправленных email (100/day на бесплатном плане)
4. **Spam проверка** - Первые несколько email могут попасть в спам, пока SendGrid не "узнает" ваш домен

## 🚀 После настройки:

1. Добавьте переменные в Vercel
2. Дождитесь автоматического деплоя
3. Протестируйте отправку email
4. Проверьте, что email доставляются корректно

## 📞 Поддержка:

Если возникли проблемы:
1. Проверьте логи в Vercel Dashboard → Functions → Logs
2. Проверьте SendGrid Dashboard → Activity → Email Activity
3. Убедитесь, что домен верифицирован
4. Проверьте, что API ключ активен и имеет правильные права

---

**Готово!** Ваша система теперь использует SendGrid для отправки email. 🎉

