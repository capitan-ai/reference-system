# Настройка Gmail для уведомлений

## 🎯 Быстрая настройка

### Шаг 1: Включить 2-Step Verification

1. Зайдите на [myaccount.google.com/security](https://myaccount.google.com/security)
2. Найдите "2-Step Verification"
3. Включите (если еще не включено)
4. Следуйте инструкциям для настройки

### Шаг 2: Создать App Password

1. Зайдите на [myaccount.google.com/apppasswords](https://myaccount.google.com/apppasswords)
2. Выберите:
   - **Select app:** "Mail"
   - **Select device:** "Other (Custom name)"
   - Введите: "Zorina Referral System"
3. Нажмите "Generate"
4. **Скопируйте 16-значный пароль** (показывается только один раз!)
   - Формат: `xxxx xxxx xxxx xxxx` (4 группы по 4 цифры)

### Шаг 3: Добавить в переменные окружения

В Vercel или `.env.local`:

**Если у вас уже есть переменные в Vercel:**
- Используйте существующие `BUSINESS_EMAIL` и `GMAIL_APP_PASSWORD`
- Или `TEST_EMAIL` и `TEST_EMAIL_PASSWORD`
- Система автоматически найдет их!

**Если настраиваете с нуля, используйте любой вариант:**

```bash
# Вариант 1: Стандартные имена
GMAIL_USER=ваш-email@gmail.com
GMAIL_APP_PASSWORD=xxxxxxxxxxxxxxxx

# Вариант 2: Использовать существующие BUSINESS_EMAIL
BUSINESS_EMAIL=ваш-email@gmail.com
GMAIL_APP_PASSWORD=xxxxxxxxxxxxxxxx

# Вариант 3: TEST_EMAIL (если уже есть в Vercel)
TEST_EMAIL=ваш-email@gmail.com
TEST_EMAIL_PASSWORD=xxxxxxxxxxxxxxxx

# Email куда отправлять уведомления (обязательно)
ADMIN_EMAIL=ваш-email@gmail.com
```

**Приоритет переменных:**
1. `GMAIL_USER` или `BUSINESS_EMAIL` или `TEST_EMAIL`
2. `GMAIL_APP_PASSWORD` или `TEST_EMAIL_PASSWORD`

## ✅ Проверка

После настройки, при следующем использовании реферального кода вы получите email на `ADMIN_EMAIL`.

## 🔍 Что если не работает?

### Проблема: "Invalid login"
- Проверьте, что используете **App Password**, а не обычный пароль
- Убедитесь, что 2-Step Verification включена

### Проблема: "Less secure app access"
- Gmail больше не поддерживает "less secure apps"
- Используйте **App Password** (см. шаг 2)

### Проблема: Email не приходит
1. Проверьте папку "Спам"
2. Проверьте логи в Vercel
3. Убедитесь, что `ADMIN_EMAIL` правильный

## 📝 Пример

```bash
# .env.local
GMAIL_USER=admin@studiozorina.com
GMAIL_APP_PASSWORD=abcd efgh ijkl mnop
ADMIN_EMAIL=admin@studiozorina.com
```

## 🔒 Безопасность

- **НЕ** коммитьте `GMAIL_APP_PASSWORD` в git
- Храните только в переменных окружения
- App Password можно отозвать в любой момент в Google аккаунте

## 💡 Советы

- Можно использовать отдельный Gmail аккаунт для уведомлений
- App Password работает только для этого приложения
- Если забыли пароль, создайте новый (старый автоматически отключится)

---

**Готово!** Теперь уведомления будут отправляться через Gmail.

