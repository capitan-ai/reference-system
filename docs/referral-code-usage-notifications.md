# Уведомления об использовании реферального кода

## 📧 Обзор

Система автоматически отправляет email уведомления администратору каждый раз, когда кто-то использует реферальный код. Уведомление содержит всю информацию о клиенте, реферере, бронировании и подаренной карте.

## ⚙️ Настройка

### 1. Настроить email администратора

Добавьте одну из следующих переменных окружения:

```bash
# Вариант 1: ADMIN_EMAIL
ADMIN_EMAIL=your-email@example.com

# Вариант 2: REFERRAL_NOTIFICATION_EMAIL (альтернативное название)
REFERRAL_NOTIFICATION_EMAIL=your-email@example.com
```

**Приоритет:** Если установлены обе переменные, используется `ADMIN_EMAIL`.

### 2. Настроить отправку email

**Вариант A: Через Gmail (рекомендуется) ⭐**

```bash
GMAIL_USER=your-email@gmail.com
GMAIL_APP_PASSWORD=your-app-password
```

**Как получить App Password:**
1. Включите 2-Step Verification в Google аккаунте
2. Создайте App Password: [myaccount.google.com/apppasswords](https://myaccount.google.com/apppasswords)
3. Скопируйте 16-значный пароль

**Вариант B: Через SendGrid**

```bash
SENDGRID_API_KEY=your-sendgrid-api-key
FROM_EMAIL=noreply@yourdomain.com
```

**Приоритет:** Если настроены оба, используется SendGrid. Если только Gmail - используется Gmail.

### 3. Проверить, что email не отключен

Убедитесь, что отправка email не отключена:

```bash
# Эти переменные НЕ должны быть установлены в 'true'
DISABLE_EMAIL_SENDING=false  # или не устанавливать
EMAIL_ENABLED=true            # или не устанавливать
```

**Подробные инструкции:**
- Gmail: `docs/gmail-setup-ru.md`
- SendGrid: `docs/sendgrid-setup-ru.md`

## 📬 Когда отправляются уведомления

Уведомления отправляются в **3 случаях**:

### 1. При создании бронирования с реферальным кодом
- **Триггер:** `booking.created` webhook
- **Когда:** Новый клиент забронировал с реферальным кодом
- **Что происходит:**
  - Клиент получает $10 gift card
  - Отправляется уведомление администратору
- **Источник в уведомлении:** `booking.created (source)`

### 2. При завершении первого платежа (реферер получает новую карту)
- **Триггер:** `payment.completed` webhook
- **Когда:** Клиент завершил первый платеж, реферер получает первую награду
- **Что происходит:**
  - Реферер получает новую $10 gift card
  - Отправляется уведомление администратору
- **Источник в уведомлении:** `payment.completed (referrer_reward)`

### 3. При завершении платежа (реферер получает загрузку на существующую карту)
- **Триггер:** `payment.completed` webhook
- **Когда:** Клиент завершил платеж, реферер получает загрузку на существующую карту
- **Что происходит:**
  - На существующую gift card реферера загружается $10
  - Отправляется уведомление администратору
- **Источник в уведомлении:** `payment.completed (referrer_reward_loaded)`

## 📋 Что содержится в уведомлении

Каждое уведомление включает:

### Основная информация
- **Реферальный код** - код, который был использован
- **Источник** - откуда был найден код (booking.created, payment.completed и т.д.)
- **Временная метка** - когда произошло событие

### Информация о клиенте
- Полное имя
- Customer ID (Square)
- Email адрес
- Телефон
- Personal code (если есть)

### Информация о реферере
- Полное имя
- Referrer ID (Square)
- Email адрес
- Personal code

### Информация о бронировании (если доступно)
- Booking ID
- Время начала
- Location ID

### Информация о gift card (если создана)
- Gift Card ID
- GAN (Gift Account Number)
- Сумма

## 📧 Формат email

Email отправляется в HTML формате с:
- Красивым дизайном
- Структурированной информацией
- Цветовыми индикаторами для важных данных
- Текстовой версией для клиентов без поддержки HTML

**Тема письма:** `🎁 Referral Code Used: [CODE] - [Customer Name]`

## 🔍 Пример уведомления

```
🎁 Referral Code Used: BOZHENA8884 - John Doe

👤 Customer Information
   Full Name: John Doe
   Customer ID: CUSTOMER123
   Email: john@example.com
   Phone: +1234567890

⭐ Referrer Information
   Full Name: Jane Smith
   Referrer ID: CUSTOMER456
   Email: jane@example.com
   Personal Code: BOZHENA8884

📅 Booking Information
   Booking ID: BOOKING789
   Start Time: January 27, 2025, 2:00 PM
   Location ID: LOCATION123

💳 Gift Card Information
   Gift Card ID: GIFT123
   GAN: 1234567890123456
   Amount: $10.00
```

## 🛠️ Технические детали

### Функция отправки
- **Файл:** `lib/email-service-simple.js`
- **Функция:** `sendReferralCodeUsageNotification(data)`
- **Экспорт:** Доступна через `require('lib/email-service-simple')`

### Места вызова
1. `app/api/webhooks/square/referrals/route.js` - `processBookingCreated()` (строка ~2838)
2. `app/api/webhooks/square/referrals/route.js` - `processPaymentCompletion()` (строка ~2071)
3. `app/api/webhooks/square/referrals/route.js` - `processPaymentCompletion()` (строка ~2147)

### Обработка ошибок
- Если отправка уведомления не удалась, процесс **не прерывается**
- Ошибка логируется в консоль
- Это позволяет системе продолжать работу даже если email не настроен

## 🧪 Тестирование

### Проверить настройку
```bash
# Проверить переменные окружения
echo $ADMIN_EMAIL
echo $SENDGRID_API_KEY
echo $FROM_EMAIL
```

### Тестовый запрос
Можно создать тестовый endpoint для проверки:

```javascript
// app/api/test-referral-notification/route.js
const { sendReferralCodeUsageNotification } = require('../../../../lib/email-service-simple')

export async function GET(request) {
  const result = await sendReferralCodeUsageNotification({
    referralCode: 'TEST123',
    customer: {
      square_customer_id: 'TEST_CUSTOMER',
      given_name: 'Test',
      family_name: 'Customer',
      email_address: 'test@example.com',
      phone_number: '+1234567890'
    },
    referrer: {
      square_customer_id: 'TEST_REFERRER',
      given_name: 'Test',
      family_name: 'Referrer',
      email_address: 'referrer@example.com',
      personal_code: 'TEST123'
    },
    source: 'test'
  })
  
  return Response.json(result)
}
```

## 📝 Логирование

Все действия логируются в консоль:

```
✅ Referral code usage notification sent to admin: admin@example.com
   Message ID: abc123
   Referral Code: BOZHENA8884
   Customer: John Doe
   Referrer: Jane Smith
```

Если email не настроен:
```
⚠️ ADMIN_EMAIL or REFERRAL_NOTIFICATION_EMAIL not configured. Skipping admin notification.
```

Если отправка отключена:
```
⏸️ Email sending is disabled. Would send referral usage notification to admin@example.com
```

## 🔒 Безопасность

- Email адрес администратора хранится в переменных окружения
- Не логируется в production
- Отправляется только через защищенное соединение (SendGrid)

## 🚀 Развертывание

### Vercel
Добавьте переменные окружения в настройках проекта:
1. Settings → Environment Variables
2. Добавьте `ADMIN_EMAIL`
3. Добавьте `SENDGRID_API_KEY` (если еще не добавлен)
4. Добавьте `FROM_EMAIL` (если еще не добавлен)

### Локальная разработка
Добавьте в `.env.local`:
```bash
ADMIN_EMAIL=your-email@example.com
SENDGRID_API_KEY=your-key
FROM_EMAIL=noreply@yourdomain.com
```

## ❓ FAQ

**Q: Можно ли отключить уведомления?**  
A: Да, просто не устанавливайте `ADMIN_EMAIL` или `REFERRAL_NOTIFICATION_EMAIL`.

**Q: Можно ли отправлять на несколько email?**  
A: В текущей версии поддерживается только один email. Можно указать несколько адресов через запятую, но лучше использовать email forwarding.

**Q: Что если SendGrid не настроен?**  
A: Уведомления будут пропущены, но система продолжит работать. В логах будет сообщение.

**Q: Как часто приходят уведомления?**  
A: Каждый раз, когда используется реферальный код. Может быть несколько уведомлений для одного использования (при бронировании и при платеже).

**Q: Можно ли настроить фильтры?**  
A: В текущей версии все уведомления отправляются. Можно настроить фильтры на стороне email клиента.

---

**Дата создания:** 2025-01-27  
**Последнее обновление:** 2025-01-27

